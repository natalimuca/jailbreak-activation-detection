[pytest]
markers =
    network: test hits the network (HF Hub / GitHub raw) to fetch real dataset data
    model: test loads a real model and runs generation on GPU (slow, needs CUDA)
testpaths = tests
