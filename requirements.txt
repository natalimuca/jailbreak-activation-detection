# Install torch separately: pip install torch --index-url https://download.pytorch.org/whl/cu130
# (CUDA build tag depends on your GPU driver -- check `nvidia-smi` and https://download.pytorch.org/whl/torch/)
torch
transformers>=4.44
nnsight>=0.3
accelerate>=0.33
bitsandbytes>=0.43; platform_system != "Darwin"
datasets>=2.20
numpy
pandas
scikit-learn
scipy
matplotlib
seaborn
tqdm
pytest
